"use client";

/**
 * @deprecated Navigation lives in the tenant header via {@link PageWayfinder}.
 * Kept as a no-op export so older imports do not break.
 */
export function FormPageNavBar(_props: { tenantSlug: string }) {
  return null;
}
